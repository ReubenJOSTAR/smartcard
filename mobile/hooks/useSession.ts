// Current session from server — placeholder.
// See mobile/CLAUDE.md → React Query (TanStack Query v5).

export function useSession(sessionId: string) {
  // TODO: useQuery(['session', sessionId], ...)
}
