// Barcode lookup, 30min stale time — placeholder.
// See mobile/CLAUDE.md → React Query (TanStack Query v5).

export function useProduct(barcode: string, storeId: number | null) {
  // TODO: useQuery(['product', barcode, storeId], ...)
}
