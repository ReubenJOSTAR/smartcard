// Home: crash recovery resume prompt, recent sessions — placeholder.
// See progress.md → Mobile — Core Screens and mobile/CLAUDE.md → Session Crash Recovery.

import { View } from 'react-native'

export default function HomeScreen(): JSX.Element {
  return <View />
}
