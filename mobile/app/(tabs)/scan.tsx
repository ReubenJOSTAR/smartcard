// Barcode scanner + live bill + budget bar — placeholder.
// See progress.md → Mobile — Core Screens and mobile/CLAUDE.md → Barcode Scanner.
// NEVER import from 'expo-barcode-scanner' — deprecated, use 'expo-camera' CameraView.

import { View } from 'react-native'

export default function ScanScreen(): JSX.Element {
  return <View />
}
