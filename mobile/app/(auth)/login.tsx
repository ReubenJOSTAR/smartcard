// Phone number entry (+91 prefix, E.164 format) — placeholder.
// See progress.md → Mobile — Auth Screens and mobile/CLAUDE.md → Screen Map.

import { View } from 'react-native'

export default function LoginScreen(): JSX.Element {
  return <View />
}
