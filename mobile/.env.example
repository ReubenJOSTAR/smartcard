# Expo reads env vars prefixed EXPO_PUBLIC_ at build time. See mobile/CLAUDE.md
# → Environment Variables. Copy to .env.local and fill in for local dev.

# Points at the local Docker API. iOS Simulator: localhost works.
# Android Emulator: use http://10.0.2.2:8000 instead.
# Physical device: use your machine's LAN IP, e.g. http://192.168.1.x:8000.
EXPO_PUBLIC_API_URL=http://localhost:8000
