// Persistent top banner when offline — placeholder.
// See progress.md → Mobile — Components and mobile/CLAUDE.md → Offline Mode Implementation.

import { View } from 'react-native'

export type OfflineBannerProps = {
  isOffline: boolean
}

export function OfflineBanner(props: OfflineBannerProps): JSX.Element | null {
  return null
}
